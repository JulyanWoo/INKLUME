<Window x:Class="Inklume.Desktop.Views.ImportChapterDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Import Chapter"
        Width="560"
        SizeToContent="Height"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Margin="0,0,0,20">
            <TextBlock Text="Import a local chapter" Style="{StaticResource PageTitleTextStyle}" />
            <TextBlock Text="Supported images will be copied into the open project in natural order."
                       Style="{StaticResource SecondaryTextStyle}" Margin="0,6,0,0" />
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,0,0,14">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="140" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <StackPanel Margin="0,0,12,0">
                <TextBlock Text="Chapter number" Margin="0,0,0,6" />
                <TextBox Text="{Binding ChapterNumber, UpdateSourceTrigger=PropertyChanged}"
                         AutomationProperties.Name="Chapter number" />
            </StackPanel>
            <StackPanel Grid.Column="1">
                <TextBlock Text="Title (optional)" Margin="0,0,0,6" />
                <TextBox Text="{Binding ChapterTitle, UpdateSourceTrigger=PropertyChanged}"
                         AutomationProperties.Name="Chapter title" />
            </StackPanel>
        </Grid>

        <StackPanel Grid.Row="2" Margin="0,0,0,8">
            <TextBlock Text="Source folder" Margin="0,0,0,6" />
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBox Text="{Binding SourceFolder, UpdateSourceTrigger=PropertyChanged}"
                         AutomationProperties.Name="Chapter source folder" />
                <Button Grid.Column="1" Content="Browse..." Command="{Binding BrowseCommand}" Margin="8,0,0,0" />
            </Grid>
        </StackPanel>

        <StackPanel Grid.Row="3" Margin="0,0,0,18">
            <TextBlock Text="PNG, JPG, JPEG, and WebP" Style="{StaticResource CaptionTextStyle}" />
            <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource ErrorBrush}"
                       TextWrapping="Wrap" Margin="0,8,0,0" />
        </StackPanel>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Command="{Binding CancelCommand}" MinWidth="90" Margin="0,0,8,0" />
            <Button Content="Import" Command="{Binding ImportCommand}" Style="{StaticResource PrimaryButtonStyle}"
                    IsDefault="True" MinWidth="90" />
        </StackPanel>
    </Grid>
</Window>
